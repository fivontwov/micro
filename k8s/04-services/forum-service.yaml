apiVersion: v1
kind: Service
metadata:
  name: forum
  namespace: microservices
  labels:
    app: forum
spec:
  type: ClusterIP
  ports:
  - port: 8081
    targetPort: 8081
    protocol: TCP
    name: http
  selector:
    app: forum
