apiVersion: v1
kind: Service
metadata:
  name: study-postgres
  namespace: microservices
  labels:
    app: study-postgres
spec:
  type: ClusterIP
  clusterIP: None   # Headless service cho StatefulSet
  ports:
  - port: 5432
    targetPort: 5432
    protocol: TCP
    name: postgres
  selector:
    app: study-postgres
