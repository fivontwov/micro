apiVersion: v1
kind: Service
metadata:
  name: notification
  namespace: microservices
  labels:
    app: notification
spec:
  type: ClusterIP
  ports:
  - port: 8083
    targetPort: 8083
    protocol: TCP
    name: http
  selector:
    app: notification
